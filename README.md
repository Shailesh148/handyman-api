## Handyman API - Local Development

### Setup

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Start the server (from the project root):

```bash
uvicorn app.main:app --reload
```

3. Open the app in your browser:

`http://localhost:8000`




